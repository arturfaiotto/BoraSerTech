const arr = [10, 20, 30, 40, 50]

for (let index = 0; index < arr.length; index++) {
    console.log(arr[index]);
}

for (let vez = 1; vez <= 10; vez++) {
    console.log("Ola, pela " + vez + "º vez");
}